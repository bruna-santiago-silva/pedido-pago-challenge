export interface IAgent {
  agent_id: number;
  branch: string;
  department: string;
  image: string;
  name: string;
  role: string;
  status: string;
}

export interface ITableHeadData {
  title: string;
  class: string;
}